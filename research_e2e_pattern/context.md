# Context: E2E Testing Pattern Research

## 1. Project Overview
- **Project Name**: `Cblack_playwright_test_framework`
- **Goal**: Research, adapt, and build a customized standard (pattern/strategy) for writing E2E tests using Playwright for this specific project.
- **Source Material**: `partern_sample/e2e_testing.md` (External best-practice template).
- **Output Artifact**: `note.md` (Will be updated step-by-step to become the final project standard document).

## 2. Approach
- **Methodology**: Divide & Conquer (Phase-by-Phase analysis to avoid AI context limits).
- **Process**:
  1. The AI Agent reads the specific section of the source material.
  2. The AI Agent analyzes its applicability to the `Cblack_playwright_test_framework`.
  3. The User & Agent mutually agree on the customized pattern.
  4. The Agent updates `note.md` with the finalized standard for that phase.
  5. Move to the next phase on the checklist.

## 3. Constraints & Guidelines
- Always refer back to this `context.md` and the `checklist.md` before starting a new prompt.
- Ensure the outcome is practically applicable to `Cblack_playwright_test_framework`, avoiding generic or overly complex patterns if not needed.
- Keep `note.md` clean, structured, and actionable for developers.
