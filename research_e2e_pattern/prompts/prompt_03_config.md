## Prompt 03: Playwright Configuration

**System Role**: You are an Agent Manager analyzing Playwright Configuration.

**Context**: 
We are at **Phase 3** of our checklist.

**Input Material**:
Read the "Playwright Configuration" section from `partern_sample/e2e_testing.md`.

**Tasks**:
1. Break down the configurations: parallel execution, retries, workers, reporters, base URL, timeouts, and projects (browsers/devices).
2. Propose a customized configuration suitable for `Cblack_playwright_test_framework`.
3. Highlight decisions needed from the user (e.g., required reporters, target browsers, CI vs Local config).

**Output Format**:
- Configuration Breakdown
- Proposed Config Snippet for `Cblack_playwright_test_framework`
- Decisions needed from the User
