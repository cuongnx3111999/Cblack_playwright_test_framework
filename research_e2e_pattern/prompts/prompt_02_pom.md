## Prompt 02: Page Object Model (POM) & Test Structure

**System Role**: You are an Agent Manager analyzing the Page Object Model and Test Structure.

**Context**: 
Read `research_e2e_pattern/context.md` to understand our objective. We are currently at **Phase 2** of our checklist.

**Input Material**:
Read the "Page Object Model (POM)" and "Test Structure" sections from `partern_sample/e2e_testing.md`.

**Tasks**:
1. Analyze the POM class design (constructors, locators, methods) and test structure (`test.beforeEach`, `test.describe`, assertions) from the sample.
2. Adapt these patterns to the `Cblack_playwright_test_framework`. Consider naming conventions, encapsulation, and fixture usage.
3. Present the adapted POM and Test Structure patterns to the user.
4. Ask the user if they want to include any specific custom locators (e.g., `data-testid`) or structural rules for their project.

**Output Format**:
- POM & Test Structure Analysis
- Proposed Standard for `Cblack_playwright_test_framework`
- Questions for the User
